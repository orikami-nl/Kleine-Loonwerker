<h2>Alle pagina's</h2>
<table>
  <% @pages.each do |page| %>
    <tr>
      <td><%= page.key.titleize %></td>
      <td><%= link_to 'edit', edit_admin_page_path(page) %></td>
    </tr>
  <% end %>
</table>
